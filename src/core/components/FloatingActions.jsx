import { ArrowUp, MessageCircle, Phone } from 'lucide-react';
export default function FloatingActions({ contact, labels, progress }) { return <><div className="progress" style={{ transform: `scaleX(${progress})` }} /><div className="floating"><a href={`tel:${contact.phone}`} aria-label={labels.phoneLabel}><Phone aria-hidden="true" /></a><a href={`https://wa.me/${contact.whatsapp}`} aria-label={labels.whatsappLabel}><MessageCircle aria-hidden="true" /></a><a href="#top" aria-label={labels.topLabel}><ArrowUp aria-hidden="true" /></a></div></>; }
