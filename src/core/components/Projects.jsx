import { useReveal } from '../hooks/useReveal';
export default function Projects({ projects, copy }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`section section--ink ${reveal.className}`} id="projects" aria-labelledby="projects-title"><div className="shell"><p className="eyebrow">{copy.eyebrow}</p><h2 id="projects-title">{copy.heading}</h2><div className="projects-grid">{projects.map((project, i) => <article className="project-card" key={project.title}><div className="project-card__media"><img src={project.image} alt="" loading="lazy" onError={e => { e.currentTarget.hidden = true; }} /><span>0{i + 1}</span></div><div><small>{project.type}{project.sample ? ` · ${copy.sampleLabel}` : ''}</small><h3>{project.title}</h3><dl><div><dt>{copy.challengeLabel}</dt><dd>{project.challenge}</dd></div><div><dt>{copy.resultLabel}</dt><dd>{project.result}</dd></div></dl></div></article>)}</div></div></section>; }
