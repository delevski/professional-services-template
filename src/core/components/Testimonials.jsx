import { Quote } from 'lucide-react';
import { useReveal } from '../hooks/useReveal';
export default function Testimonials({ testimonials, copy }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`section section--light ${reveal.className}`} aria-labelledby="testimonials-title"><div className="shell"><p className="eyebrow">{copy.eyebrow}</p><h2 id="testimonials-title">{copy.heading}</h2><div className="testimonial-row">{testimonials.map(item => <blockquote key={item.name}><Quote aria-hidden="true" /><p>“{item.quote}”</p><footer>{item.name}<small>{copy.sampleLabel}</small></footer></blockquote>)}</div></div></section>; }
