import { useState } from 'react';
import { Plus } from 'lucide-react';
import { useReveal } from '../hooks/useReveal';
export default function Faq({ items, copy }) { const [open, setOpen] = useState(null); const reveal = useReveal(); return <section ref={reveal.ref} className={`section ${reveal.className}`} id="faq" aria-labelledby="faq-title"><div className="shell faq-grid"><div><p className="eyebrow">{copy.eyebrow}</p><h2 id="faq-title">{copy.heading}</h2><p>{copy.description}</p></div><div>{items.map((item, i) => <div className="faq-item" key={item.question}><h3><button type="button" aria-expanded={open === i} aria-controls={`faq-${i}`} onClick={() => setOpen(open === i ? null : i)}>{item.question}<Plus aria-hidden="true" /></button></h3><div id={`faq-${i}`} hidden={open !== i}><p>{item.answer}</p></div></div>)}</div></div></section>; }
