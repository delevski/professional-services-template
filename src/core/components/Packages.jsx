import { Check } from 'lucide-react';
import Button from './Button';
export default function Packages({ packages, onQuote }) { return <section className="section" id="packages" aria-labelledby="packages-title"><div className="shell"><p className="eyebrow">שקיפות מתחילה כאן</p><h2 id="packages-title">מחירים התחלתיים</h2><div className="card-grid card-grid--three">{packages.map((item, i) => <article className={`price-card${i === 1 ? ' is-featured' : ''}`} key={item.name}><span>0{i + 1}</span><h3>{item.name}</h3><strong>{item.price}</strong><p><Check aria-hidden="true" />{item.description}</p><Button variant={i === 1 ? 'primary' : 'outline'} onClick={onQuote}>לקבלת הצעה</Button></article>)}</div><p className="fine-print">המחירים באתר הם לדוגמה בלבד. מחיר סופי ייקבע לאחר אבחון.</p></div></section>; }
