import { Check } from 'lucide-react';
import Button from './Button';
import { useReveal } from '../hooks/useReveal';
export default function Packages({ packages, copy, onQuote }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`section ${reveal.className}`} id="packages" aria-labelledby="packages-title"><div className="shell"><p className="eyebrow">{copy.eyebrow}</p><h2 id="packages-title">{copy.heading}</h2><div className="card-grid card-grid--three">{packages.map((item, i) => <article className={`price-card${i === 1 ? ' is-featured' : ''}`} key={item.name}><span>0{i + 1}</span><h3>{item.name}</h3><strong>{item.price}</strong><p><Check aria-hidden="true" />{item.description}</p><Button variant={i === 1 ? 'primary' : 'outline'} onClick={onQuote}>{copy.actionLabel}</Button></article>)}</div><p className="fine-print">{copy.note}</p></div></section>; }
