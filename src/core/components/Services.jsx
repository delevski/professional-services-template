import { ArrowLeft, Bolt, CircuitBoard, Lightbulb, Car, HousePlug, BriefcaseBusiness } from 'lucide-react';
const icons = [Bolt, CircuitBoard, Lightbulb, Car, HousePlug, BriefcaseBusiness];
export default function Services({ services, onQuote }) { return <section className="section" id="services" aria-labelledby="services-title"><div className="shell"><p className="eyebrow">מומחיות מקצה לקצה</p><h2 id="services-title">שירותים</h2><div className="card-grid card-grid--three">{services.map((service, i) => { const Icon = icons[i % icons.length]; return <article className="service-card" key={service.id}><Icon aria-hidden="true" /><h3>{service.title}</h3><p>{service.description}</p><button className="text-button" onClick={onQuote}>{service.cta}<ArrowLeft aria-hidden="true" /></button></article>; })}</div></div></section>; }
