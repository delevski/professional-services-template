import { ArrowLeft } from 'lucide-react';
import ProfileIcon from './ProfileIcon';
import { useReveal } from '../hooks/useReveal';
export default function Services({ services, copy, onQuote }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`section ${reveal.className}`} id="services" aria-labelledby="services-title"><div className="shell"><p className="eyebrow">{copy.eyebrow}</p><h2 id="services-title">{copy.heading}</h2><div className="card-grid card-grid--three">{services.map(service => <article className="service-card" key={service.id}><ProfileIcon name={service.icon} aria-hidden="true" /><h3>{service.title}</h3><p>{service.description}</p><button type="button" className="text-button" onClick={onQuote}>{service.cta}<ArrowLeft aria-hidden="true" /></button></article>)}</div></div></section>; }
