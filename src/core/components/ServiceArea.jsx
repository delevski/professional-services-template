import { Clock, MapPin, Siren } from 'lucide-react';
import { useReveal } from '../hooks/useReveal';
export default function ServiceArea({ serviceArea, copy }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`section section--light ${reveal.className}`} aria-labelledby="area-title"><div className="shell area-grid"><div><p className="eyebrow">{copy.eyebrow}</p><h2 id="area-title">{serviceArea.heading}</h2><div className="area-list">{serviceArea.areas.map(area => <span key={area}><MapPin aria-hidden="true" />{area}</span>)}</div></div><aside className="availability"><p><Clock aria-hidden="true" /><span><strong>{copy.hoursLabel}</strong>{serviceArea.hours}</span></p><p><Siren aria-hidden="true" /><span><strong>{copy.urgentLabel}</strong>{serviceArea.urgentNote}</span></p></aside></div></section>; }
