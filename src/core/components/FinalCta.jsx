import { ArrowLeft, MessageCircle } from 'lucide-react';
import Button from './Button';
import { useReveal } from '../hooks/useReveal';
export default function FinalCta({ profile, copy, onQuote }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`final-cta ${reveal.className}`} id="contact" aria-labelledby="contact-title"><div className="shell"><p className="eyebrow">{copy.eyebrow}</p><h2 id="contact-title">{profile.contact.heading}</h2><p>{profile.hero.description}</p><div className="actions"><Button aria-label={profile.ui.quote.finalLabel} onClick={onQuote}>{profile.hero.primaryAction}<ArrowLeft aria-hidden="true" /></Button><a className="button button--ghost" href={`https://wa.me/${profile.contact.whatsapp}`}><MessageCircle aria-hidden="true" />{profile.hero.whatsappAction}</a></div></div></section>; }
