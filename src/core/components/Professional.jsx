import { BadgeCheck } from 'lucide-react';
import { useReveal } from '../hooks/useReveal';
export default function Professional({ professional, copy, placeholderInitials }) { const reveal = useReveal(); return <section ref={reveal.ref} className={`section section--ink ${reveal.className}`} id="about" aria-labelledby="about-title"><div className="shell about-grid"><div className="portrait"><img src={professional.portrait} alt={professional.portraitAlt} loading="lazy" onError={e => { e.currentTarget.hidden = true; }} /><span aria-hidden="true">{placeholderInitials}</span></div><div><p className="eyebrow">{copy.eyebrow}</p><h2 id="about-title">{professional.heading}</h2><p className="large-copy">{professional.biography}</p><div className="stats">{professional.stats.map(stat => <div key={stat.label}><strong>{stat.value}</strong><span>{stat.label} — {copy.sampleSuffix}</span></div>)}</div><ul className="credentials">{professional.credentials.map(item => <li key={item}><BadgeCheck aria-hidden="true" />{item}</li>)}</ul></div></div></section>; }
