import { BadgeCheck } from 'lucide-react';
export default function Professional({ professional }) { return <section className="section section--ink" id="about" aria-labelledby="about-title"><div className="shell about-grid"><div className="portrait"><img src={professional.portrait} alt={professional.portraitAlt} loading="lazy" onError={e => { e.currentTarget.hidden = true; }} /><span aria-hidden="true">OL</span></div><div><p className="eyebrow">המקצוען מאחורי העבודה</p><h2 id="about-title">{professional.heading}</h2><p className="large-copy">{professional.biography}</p><div className="stats">{professional.stats.map(stat => <div key={stat.label}><strong>{stat.value}</strong><span>{stat.label} — לדוגמה</span></div>)}</div><ul className="credentials">{professional.credentials.map(item => <li key={item}><BadgeCheck aria-hidden="true" />{item}</li>)}</ul></div></div></section>; }
